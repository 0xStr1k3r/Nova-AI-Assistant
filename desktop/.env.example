# Nova Desktop - Optional overrides
# Leave empty to auto-detect from parent .env (PORT=22222)

# Override the WebSocket server URL (useful for remote/LAN connections)
# NOVA_SERVER_URL=ws://localhost:22222/live

# Override the REST API URL  
# NOVA_API_URL=http://localhost:22222
